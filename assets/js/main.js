import { initScrollReveal } from "./scrollReveal.js";
import { typeWrite } from "./typeWrite.js";
import { hoverChangeDescription } from "./hoverChangeDescription.js";
import { menu } from "./menu.js";

menu();
initScrollReveal();
typeWrite(document.querySelector(".typewriter"));

hoverChangeDescription(
  ".python",
  "An experience of 4 years with Python programming language along with the environments like VS-Code, PyCharm, Spyder and Jupyter Lab."
);
hoverChangeDescription(
  ".sql",
  "An experience of 5 years with SQL programming language on tools like DataGrip, MS-SQL Server, SQLite and Oracle-SQL."
);
hoverChangeDescription(
  ".tab",
  "An experience of 1 year with creating insightful dashboards using Tableau."
);
hoverChangeDescription(
  ".pandas",
  "Using Pandas to manipulate the tabular data in an efficient way."
);
hoverChangeDescription(
  ".numpy",
  "Using NumPy to work with multi-dimensional arrays and matrices and perform scientific computation."
);
hoverChangeDescription(
  ".qgis",
  "Quantum Geographic Information System: To visualize, edit, process and analyze Geospatial Data."
);
hoverChangeDescription(
  ".git",
  "A version control system to keep track of the changes in codebase in a Software Development Lifecycle."
);
hoverChangeDescription(
  ".r",
  "An experience of 1 year of working with R using RStudio."
);
hoverChangeDescription(
  ".run",
  "Participated in the yearly Pune International Marathon."
);
hoverChangeDescription(
  ".green",
  "Conducted environment cleanliness awareness drives across the city to educate underprivileged students about the importance of clean surroundings and steps needed to make a difference around us."
);
hoverChangeDescription(
  ".cook",
  "I enjoy cooking and love to try new cuisines."
);
hoverChangeDescription(
  ".med",
  "To keep it all together!!"
);
hoverChangeDescription(
  ".mail",
  "To keep it all together!!"
);
hoverChangeDescription(
  ".in",
  "To keep it all together!!"
);
